name: 'finflow'
version: '1.0.0'
config-version: 2

profile: 'finflow'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

models:
  finflow:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: view
      +schema: analytics
    marts:
      +materialized: table
      +schema: analytics
